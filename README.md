 ласс на €зыке C++, который генерирует HTML-таблицу на основе
   входных данных.  ласс может быть инициализирован двумерным массивом чисел,
   который можно передать в конструктор. “акже класс можно использовать без
   инициализации его массивом, а посредством указани€ значени€ любой €чейки
   вручную. ” класса должна быть функци€ установки значени€ любой €чейки,
   метод указани€ размеров таблицы, значение €чейки равно 0 по умолчанию, если
   иное не было указано пользователем. ƒл€ генерации HTML-таблицы предусмореть
   метод, который возвращает HTML-код в виде строки: "<table>...</table>".